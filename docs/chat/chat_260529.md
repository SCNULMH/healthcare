# Chat Log 260529

## UI 입력 문구 정리
- 요청: `주간 운동 횟수`, `주간 아침식사 횟수`, `주간 단 음료`처럼 어색한 라벨을 정리하고, 일주일 기준 설명을 입력칸 쪽으로 분리.
- 변경:
  - `static/index.html`
    - 활동 입력 라벨을 `걸음수`, `운동`으로 축약.
    - 생활 입력 라벨을 `아침식사`, `단 음료`, `야식`, `외식`, `수면시간`으로 축약.
    - 각 입력칸 아래에 `최근 일주일 동안 ... 적어주세요.` 형태의 도움말 추가.
  - `static/styles.css`
    - `field-label`, `field-help` 스타일 추가.
- 유지:
  - `avg_steps`, `exercise_per_week`, `breakfast_per_week` 등 API/JS 필드명은 변경하지 않음.
  - 위험예측 로직과 저장/분석 계약은 변경하지 않음.
- 검증:
  - `node --check static/app.js`: 통과.
  - `python -m unittest discover -s tests`: 24개 통과.
- 남은 논의:
  - 아이콘/이모지 체계는 현재 기능 영향 없이 유지. UI 회의 후 전체 섹션 카드 아이콘을 유지할지, 더 미니멀하게 정리할지 결정 가능.
