# 2026-05-21 작업 기록

## 챗: 진행상황 및 분석모델 확인

### 진행상황
- 최신 원격 커밋은 `46c5a51 docs: prepare final submission qa` 기준으로 확인.
- 작업 시작 시 `main...origin/main` 동기화 상태였고, 작업트리는 깨끗했음.
- 공공데이터 API 키 연결 상태 확인 완료.
  - `PUBLIC_DATA_SERVICE_KEY configured: True`
  - `국민건강보험공단_건강검진정보` 샘플 조회 가능
  - API 응답 기준 전체 건수 1,000,000건 확인

### 분석모델 확인
- 현재 웹앱 `/risk/predict`는 `app/services/diagnosis.py`의 규칙 기반 위험예측 엔진을 실제 사용.
- 규칙 기반 엔진은 당뇨, 고혈압, 이상지질혈증 위험도를 모두 산출하고 개인 맞춤 개선 행동을 생성.
- `scripts/train_risk_models.py`는 공공데이터 CSV 기반 RandomForest/LogisticRegression 학습 파이프라인으로 구현되어 있음.

### 발견한 문제
- 기존 학습 스크립트는 지질 수치 결측이 있으면 `dropna()`로 전체 행을 제거해, 샘플 CSV에서 학습 데이터가 0건이 되는 문제가 있었음.
- 공공데이터 API에서 내려받은 건강검진정보 3,000건 표본은 총콜레스테롤, HDL, LDL, 중성지방 값이 모두 비어 있어 이상지질혈증 학습 라벨을 만들 수 없었음.
- 당뇨/고혈압 모델의 높은 성능은 공복혈당·혈압 기준으로 만든 라벨을 같은 검진 수치로 분류하기 때문이므로, 장기 질병 발생 예측 성능으로 설명하면 안 됨.

### 수정 내용
- `scripts/train_risk_models.py`
  - 결측 feature 전체 제거 대신 target 라벨 기준으로만 필터링하도록 수정.
  - 수치형 변수에 median imputer 적용.
  - 전부 결측인 수치 컬럼도 feature shape가 유지되도록 `keep_empty_features=True` 적용.
  - 학습 표본 부족, 단일 클래스, 소수 클래스 부족 시 실패하지 않고 `skipped` 리포트로 기록하도록 수정.
  - 이상지질혈증은 지질 수치가 있는 행에서만 라벨을 생성하도록 수정.
- `docs/ai_model_strategy.md`
  - 현재 라벨은 검진 수치 기준 위험군 분류이며 미래 질병 발생 예측이 아니라는 주의 문구 추가.
  - 지질 수치 결측 시 이상지질혈증 모델은 학습 제외되고 MVP 규칙 기반 산출을 유지한다는 설명 추가.
- `docs/model_check_260521.md`
  - 현재 앱 모델, 규칙 기준, 학습 파이프라인, 실제 데이터 확인 결과, 제출용 표현을 별도 문서로 정리.

### 검증 결과
- `python scripts/test_public_data_key.py`: 공공데이터 API reachable 확인.
- `python scripts/fetch_public_data.py --dataset checkup --pages 3 --per-page 1000 --out .\data\health_checkup_training_3000.csv`: 3,000건 저장.
- `python scripts/train_risk_models.py --csv .\data\health_checkup_training_3000.csv --out .\models\risk_models_3000.joblib`
  - `diabetes_risk`: trained, rows 3000, class_counts `0=2844`, `1=156`
  - `hypertension_risk`: trained, rows 3000, class_counts `0=2612`, `1=388`
  - `dyslipidemia_risk`: skipped, rows 0
- 학습된 모델 로드 및 데모 입력 예측 확인.
- `python -m py_compile scripts/train_risk_models.py scripts/fetch_public_data.py scripts/test_public_data_key.py`: 통과.
- `python -m unittest discover -s tests`: 9개 테스트 통과.
- `node --check static/app.js`: 문법 오류 없음.
- 변경 파일 UTF-8 replacement 문자 없음 확인.

### 다음 작업
- 제출자료에는 "규칙 기반 MVP + 학습 가능 파이프라인"으로 설명.
- 당뇨/고혈압 성능 수치는 "검진 기준 위험군 분류"로만 해석.
- 이상지질혈증 학습 모델은 지질 수치가 포함된 CSV를 별도로 확보하기 전까지 규칙 기반 MVP로 유지.
- Render 배포 URL에서 최신 커밋 반영 여부와 UI 동작 직접 확인.

## AI 활용 강조 보강 기록

### 수행 내용
- `/risk/predict` 응답에 `ai_explanation` 필드 추가.
  - `title`: `AI가 이렇게 판단했어요`
  - `steps`: 위험도 예측, 위험 요인 설명, 개인 맞춤 개선 플랜
  - `model_note`: 설명 가능한 규칙 기반 AI 엔진과 RandomForest/LogisticRegression 학습 파이프라인 설명
- 결과 화면에 `AI가 이렇게 판단했어요` 섹션 추가.
- 질환별 위험도 카드에 `AI 판단 근거` 문구 추가.
- 행동 플랜 섹션 제목을 `AI 개인화 추천`으로 변경.
- CSS/JS 캐시 회피 버전을 `260521-ai1`로 갱신.
- README에 `AI 활용 3단계` 섹션 추가.
- `docs/submission_form_draft.md`, `docs/ai_model_strategy.md`, `docs/model_check_260521.md`에 심사용 AI 강조 문장 추가.

### 검증 결과
- `python -m unittest discover -s tests`: 9개 테스트 통과.
- `node --check static/app.js`: 문법 오류 없음.
- `python -m py_compile app/routers/risk.py scripts/train_risk_models.py scripts/fetch_public_data.py scripts/test_public_data_key.py`: 통과.
- FastAPI TestClient로 `/risk/predict` 응답에 `ai_explanation` 포함 확인.
- `/`, `/static/styles.css?v=260521-ai1`, `/static/app.js?v=260521-ai1` 200 응답 확인.
- 변경 파일 UTF-8 replacement 문자 없음 확인.

### 다음 작업
- Render Dashboard에서 최신 커밋 배포 여부 확인.
- 배포 URL에서 분석 결과 화면의 AI 설명 카드가 보이는지 확인.
- 제출서류에는 AI를 과장하지 않고 `설명 가능한 AI MVP + 학습 파이프라인 + 개인화 행동 변환`으로 표현.

## 학습 모델 런타임 분기 구현 기록

### 수행 내용
- `RISK_MODEL_MODE`, `RISK_MODEL_PATH` 설정 추가.
- `app/services/model_runtime.py` 추가.
  - `models/risk_models.joblib` 또는 지정 경로에서 joblib 모델 번들 로드.
  - `auto` 모드에서는 모델 파일이 있으면 학습 모델 사용, 없으면 규칙 기반 fallback.
  - `rule` 모드에서는 항상 규칙 기반 엔진 사용.
  - `model` 모드에서는 모델 파일 로드 실패 시 `/risk/predict`가 503 반환.
- `/risk/metadata`에 모델 상태 추가.
- `/risk/predict` 응답에 `engine` 필드 추가.
- 결과 화면에 현재 사용 예측 엔진 문구 추가.
- `USE_DEMO_DATA=true`이면 공공데이터 API 대신 데모 샘플을 반환하도록 변경.
- 공공데이터 API 호출 실패 시 데모 샘플로 fallback하도록 변경.
- `docs/colab_model_training.md` 추가.

### 검증 결과
- 기본 `auto` 모드 + 기본 경로 모델 없음: 규칙 기반 엔진으로 정상 예측.
- `RISK_MODEL_MODE=auto`, `RISK_MODEL_PATH=models/risk_models_3000.joblib`: 모델 로드 성공, 당뇨·고혈압 학습 모델 사용.
- `RISK_MODEL_MODE=model`, 잘못된 모델 경로: `/risk/predict` 503 반환.
- `USE_DEMO_DATA=true`: `/health/public-data`가 demo 상태 반환, `/data/checkup/prefill`이 데모 공공데이터 행으로 mapped 응답.
- `python -m unittest discover -s tests`: 9개 테스트 통과.
- `node --check static/app.js`: 문법 오류 없음.
- `python -m py_compile app/core/config.py app/routers/risk.py app/services/model_runtime.py app/services/public_data.py scripts/train_risk_models.py scripts/fetch_public_data.py scripts/test_public_data_key.py`: 통과.

### 운영 권장
- 제출/Render 무료 환경: `RISK_MODEL_MODE=auto` 또는 `rule`, `USE_DEMO_DATA=true`.
- 다른 호스팅에서 모델 파일 테스트: `RISK_MODEL_MODE=auto`, `RISK_MODEL_PATH`를 업로드된 joblib 경로로 지정.
- 강제 검증: `RISK_MODEL_MODE=model`로 설정해 파일 누락/로드 실패를 503으로 확인.

## OCR 실제 연동 구조 구현 기록

### 수행 내용
- 기존 `/risk/ocr/extract`의 데모 stub을 provider 기반 OCR 런타임으로 교체.
- `app/services/ocr_runtime.py` 추가.
  - 기본 `OCR_PROVIDER=demo`에서는 기존처럼 데모 검진값 반환.
  - `OCR_PROVIDER=openai`와 `OPENAI_API_KEY` 설정 시 OpenAI Responses API로 이미지/PDF OCR 호출.
  - 이미지 파일은 base64 data URL, PDF는 base64 file input으로 전달.
  - 업로드 파일은 서버 파일시스템에 저장하지 않고 메모리에서만 처리.
  - OCR 결과 JSON을 검진 입력 스키마로 매핑하고 허용 범위 검증 후 반영.
  - 키 미설정, OCR 실패, 미지원 provider는 `OCR_FALLBACK_TO_DEMO=true`일 때 데모 값으로 fallback.
- `/risk/metadata`에 OCR provider 상태 추가.
- 프론트 OCR 상태 메시지에 provider와 파일명을 표시하도록 수정.
- CSS/JS 캐시 회피 버전을 `260521-ocr1`로 갱신.
- `.env.example`, `render.yaml`, README, OCR 문서, 제출서류 초안에 OCR 환경변수와 제출 표현 반영.

### 검증 결과
- `python -m unittest discover -s tests`: 10개 테스트 통과.
- `node --check static/app.js`: 문법 오류 없음.
- `python -m py_compile app/core/config.py app/routers/risk.py app/services/ocr_runtime.py app/services/model_runtime.py app/services/public_data.py scripts/train_risk_models.py scripts/fetch_public_data.py scripts/test_public_data_key.py`: 통과.
- 기본 데모 모드에서 `/risk/metadata`, `/risk/ocr/demo`, `/risk/ocr/extract` 정상 응답 확인.
- `OCR_PROVIDER=openai`, `OPENAI_API_KEY` 없음: `ocr_fallback_demo`로 안전하게 전환 확인.
- `OCR_PROVIDER=openai`, 잘못된 API 키: OpenAI API 오류 후 `ocr_fallback_demo`로 안전하게 전환 확인.

### 다음 작업
- 실제 OpenAI API 키와 샘플 검진 결과지 이미지/PDF로 추출 정확도 확인.
- OCR 결과 사용자 확인·수정 화면을 별도 단계로 분리하면 제출 완성도가 더 올라감.
- Render 자동 배포는 `render.yaml`에 `autoDeploy: true`, `autoDeployTrigger: commit`으로 설정되어 있으나 Dashboard 권한이 필요해 실제 토글 상태는 Render 화면에서 확인 필요.
