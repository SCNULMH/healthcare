# 2026-05-28 작업 기록

## 챗 기록 규칙 도입

### 진행상황
- 의료 공모전 앱은 `검진AI 리셋코치` 기준으로 Firebase, OCR, 결측치 fallback, 결과 저장, 결과 탭 UI까지 구현된 상태.
- 최신 주요 커밋: `b20f1e2 Harden medical fallback and OCR unknown handling`.
- 검증 기준:
  - `python -m unittest discover -s tests`: 23개 통과
  - `node --check static/app.js`: 통과
- 현재 서버 기준 주소: `http://127.0.0.1:8004`

### 이번 하네스 작업
- 의료 앱 전용 `AGENTS.md` 추가.
- 기존 `.codex/AGENTS.md`의 잘못된 농업 프로젝트 문맥을 의료 앱 전용 지침으로 교체.
- `.codex/hooks.json`을 repo-local `scripts/harness_context.py` 호출 구조로 교체.
- `TODO.json` 추가로 현재 목표, 완료 항목, 남은 작업을 기계 판독 가능하게 분리.
- `scripts/repo_map.py` 추가로 긴 Python 파일을 전체 읽지 않고 class/function 구조만 확인 가능.
- `docs/harness_architecture.md`에 하네스 설계 기록.

### Macro Command
- `챗`: 이 파일의 마지막 섹션과 `TODO.json`을 읽고 이전 기록 및 남은 작업을 이어간다.
- `챗 기록`: 현재 진행상황, 변경 파일, 테스트 결과, blocker, 다음 작업을 이 파일에 append한다.
- `저장`: 테스트 후 문서 갱신, commit, push까지 수행한다.

### 남은 과정
- T001: `/risk/save-result` 서버단 Firebase ID token 또는 세션 토큰 검증 추가.
- T002: Render 실배포 환경변수와 health/account/OCR 확인.
- T003: 모바일 UI 최종 캡처와 저장 흐름 확인.
- T004: 사업계획서에 결측치 fallback, OCR 비저장, Firebase 보안 흐름, 모델 신뢰도 설명 반영.

## 챗 복원 후 T001 진행

### 이전 기록
- `TODO.json` 기준 active goal은 의료 공모전 제출용 웹앱 완성도 보강 및 Render/Firebase 배포 준비.
- 직전 주요 커밋은 `17b887e Add project harness directives and chat continuity`.
- 남은 작업은 T001 서버단 저장 인증, T002 Render 점검, T003 모바일 UI 캡처, T004 제출 문서 보강.

### 수행 내용
- `/account/register`, `/account/login` 응답에 서버 서명 `session_token`을 포함.
- `static/app.js`의 진단결과 저장 payload에 `user_id`, `session_token`을 포함.
- 프로필 저장 후에도 기존 로그인 토큰이 유지되도록 보존.
- `/risk/save-result`에서 `user_id + session_token`을 검증하고, 누락/불일치 시 401/403으로 저장 차단.
- `.env.example`, `render.yaml`에 `SESSION_SECRET`, `SESSION_TTL_SECONDS` 추가.
- `TODO.json`에서 T001 완료 처리.

### 검증 결과
- `python -m unittest discover -s tests`: 24개 통과.
- `node --check static/app.js`: 통과.
- `python -m py_compile app/core/config.py app/services/account_store.py app/routers/account.py app/routers/risk.py`: 통과.
- 로컬 서버 `http://127.0.0.1:8004` 재시작 후 토큰 없는 `/risk/save-result` 호출이 401과 로그인 안내를 반환하는 것 확인.

### 남은 과정
- T002: Render 실배포 환경변수와 health/account/OCR 확인.
- T003: 모바일 UI 최종 캡처와 저장 흐름 확인.
- T004: 사업계획서에 결측치 fallback, OCR 비저장, Firebase 보안 흐름, 모델 신뢰도 설명 반영.

## 제출서류 작성 단계 진입

### 수행 내용
- `서류/`의 공고문 및 제출 양식 HWP/PDF를 `kordoc`으로 Markdown 추출.
- 제출 양식에서 사업계획서 8페이지 이내, 제품 및 서비스 개발 부문 시제품 증빙 필수 조건 확인.
- `docs/submission_answer_draft_260528.md` 작성:
  - 참가신청서용 사업명/사업내용/공공데이터 활용목록/기대효과 문구
  - 사업계획서 1~6번 문항별 답변 초안
  - 시제품 증빙 캡처 목록과 설명 문구
- `docs/submission_fill_checklist_260528.md` 작성:
  - 직접 기입해야 하는 개인정보/팀 정보
  - 평가기준 대응 포인트
  - 제출 전 확인 항목

### 다음 과정
- 사용자 팀명, 대표자, 소속, 연락처, 수상/특허 이력 정보를 받아 참가신청서와 6번 역량 항목 최종화.
- Render URL 확정 후 시제품 증빙 자료에 URL과 캡처 설명 반영.
- 사업계획서 8페이지 분량에 맞게 문장 압축.
